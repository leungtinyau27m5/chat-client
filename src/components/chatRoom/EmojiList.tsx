import { Box, styled } from "@mui/material";

const StyledBox = styled(Box)(() => ({
  display: "flex",
  flexWrap: "wrap",
}));

const EmojiList = () => {
  const codes = [128512, 129488]
  return <StyledBox></StyledBox>;
};

export default EmojiList;
